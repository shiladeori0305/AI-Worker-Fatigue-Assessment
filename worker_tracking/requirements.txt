# ──────────────────────────────────────────────────────────────────
# requirements.txt — Module 1: Worker Detection and Tracking
# Python 3.11+
# Install with:  pip install -r requirements.txt
# ──────────────────────────────────────────────────────────────────

# Core vision / ML
ultralytics>=8.2.0          # YOLOv8 + built-in ByteTrack integration
opencv-python>=4.9.0        # Video I/O and frame annotation
numpy>=1.26.0               # Array operations

# ByteTrack ships inside ultralytics; no separate install needed.
# PyYAML is a transitive dependency of ultralytics (already installed).

# Optional: GPU acceleration (uncomment if CUDA is available)
# torch>=2.2.0+cu121
# torchvision>=0.17.0+cu121